const encabezado = document.querySelector(".contenido-hero h1").textContent;

console.log(encabezado);
// console.log(encabezado.innerText); // Si en el CSS - Visibility:hidder, no lo va a encontrar
// console.log(encabezado.textContent); // Si lo va a encontrar
// console.log(encabezado.innerHTML); // Se trae el HTML

// const newheading = "Nuevo Heading";
// document.querySelector(".contenido-hero h1").textContent = newheading;

// const img = document.querySelector(".card img");
// img.src = "img/hacer2.jpg";
